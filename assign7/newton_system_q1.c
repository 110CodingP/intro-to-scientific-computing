#include <stdio.h>

void solve(int num_eq,double x_init,double TOL,int max_iter) {
    for (int i=1;i<=max_iter;i++) {
        
    }
}

int main() {
    //input
    int num_eq=0;
    scanf("%d",&num_eq);
    double x_init=0.0;
    scanf("%lf",&x_init);
    double TOL=0.0;
    scanf("%lf",&TOL);
    int max_iter=0;
    scanf("%d",&max_iter);

    solve(num_eq,x_init,TOL,max_iter);
    return 0;
}
